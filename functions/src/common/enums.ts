export enum DayOfWeek {
    Sunday,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
}

export enum AssetType {
    Video,
    Image,
    Audio,
    Icon
}

export enum EntityType {
    Shop,
    Restaurant,
}
export enum HttpStatusCode {
    NotAuthorizate = 403,
    NotFound = 404,
    Error = 500,
    Success = 200
}